Challenge 5.docx
CMS Challenge - Case Bundle (Synthetic)/

# Secrets
.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# Stage 1 derived data (regenerated by build_chunks.py; registry.json is NOT ignored)
chunks.json
chunk_stats.json

# Stage 2 derived data (regenerated by build_propositions.py)
chunks_with_embeddings.json
propositions.json
# Mode B outputs are intentionally tracked (needed for the demo side panel and
# the Stage 3 chunk-overlap cross-check): propositions_mode_b.json, mode_b_validation.json

# Stitch design export (reference only — not part of the app)
stitch_pleading_to_proof_ui/
