Challenge 5.docx

# Source bundle (the 20 synthetic .docx)
bundle/

# Secrets
.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# Stage 1 derived data (regenerated by pipeline/build_chunks.py; data/registry.json is NOT ignored)
data/chunks.json
data/chunk_stats.json

# Stage 2 derived data (regenerated by pipeline/build_propositions.py)
data/chunks_with_embeddings.json
data/propositions.json
# Other data/*.json are intentionally tracked (retrieval/classification/scoring/graph/demo +
# the Mode B outputs propositions_mode_b.json, mode_b_validation.json, and registry.json)

# Stitch design export (reference only — not part of the app)
stitch_pleading_to_proof_ui/
